#cloud-config
system_info:
  default_user:
    name: {{ regular_system_user }}
chpasswd:
  list: |
    root:{{ regular_system_user }}
    {{ regular_system_user }}:{{ regular_system_user }}
  expire: False
packages:
  - python
