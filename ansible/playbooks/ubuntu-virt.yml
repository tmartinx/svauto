---

#
# Virtualization Playbook for Basic Infra
#

- hosts: metais
  become: yes
  roles:
  - download-images
  - { role: ubuntu-virt, setup: 'host' }
  - { role: ubuntu-virt, setup: 'vnets', when: vnets is defined }
  - { role: ubuntu-virt, setup: 'instances', when: instances is defined }
